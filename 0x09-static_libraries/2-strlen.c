#include "main.h"

/**
 * _strlen - counts the lenght of a string@s: string pointer
 * @s: string pointer
 *
 * Return: the lenght
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0' ; i++)
	{
	}
	return (i);
}
