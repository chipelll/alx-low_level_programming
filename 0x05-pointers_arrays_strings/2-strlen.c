#include "main.h"

/**
 * _strlen - counts the lenght of a string@s: string pointer
 * @s: string pointer
 *
 * Return: the lenght
 */

int _strlen(char *s)
{
	int i = 0;

	for ( ; s[i] != '\0' ; i++)
	{
	}
	return (i);
}
