#include <stdio.h>
/**
 * Main function serves as a starter
 * Void: This means nothing is available to fill up the space
 *
 * The main function serves as a pointer for the program to
 * know where to start coding from
 * Puts: This helps to print out a statement
 * Return: 0 if the program is an error
 */
int main(void)
{
	char Program[] = "\"Programming is like building a multilingual puzzle";
	puts(Program);
	return (0);
}
